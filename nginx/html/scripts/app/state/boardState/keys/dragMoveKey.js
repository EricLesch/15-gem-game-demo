/**
 * Enumeration which represents the data parameters on the DRAG_MOVE action
 * @type {{NUMBER: string, X: string, Y: string}}
 */
const DRAG_MOVE_KEY = {
    NUMBER: "NUMBER",
    X: "X",
    Y: "Y"
};

module.exports = DRAG_MOVE_KEY;